# 后续开发计划 — oms-wms-tms 生产就绪路线图

**生成时间**: 2026-07-30  
**项目现状**: 核心功能 ~85% 完成，可用于演示/POC  
**目标**: 达到国内中小物流企业生产可用标准

---

## 总览路线图

```
Phase 6 (生产加固)     ████████████░░░░░░  Week 1-2    ⬅ 当前
Phase 7 (电子面单)     ░░░░░░░░░░░░░░░░░░  Week 3-4
Phase 8 (快递对接)     ░░░░░░░░░░░░░░░░░░  Week 5-6
Phase 9 (派件管理)     ░░░░░░░░░░░░░░░░░░  Week 7-8
Phase 10 (通知)        ░░░░░░░░░░░░░░░░░░  Week 7-8（与 9 并行）
Phase 11 (部署/运维)   ░░░░░░░░░░░░░░░░░░  Week 1-8（持续）
Phase 12 (测试覆盖)    ░░░░░░░░░░░░░░░░░░  Week 1-8（持续）
```

---

## 🔴 Phase 6 — 生产加固（高优，Week 1-2）

### 6.1 基础设施简化（降低门槛）

中小物流企业很少有专人维护 Redis/RabbitMQ，需提供降级方案。Celery 以 Redis 为 broker，Outbox 走 HTTP 派发，无需 RabbitMQ。

| 任务 | 内容 | 文件 |
|------|------|------|
| 6.1.1 Redis 降级 | 无 Redis 时回退到内存缓存 | `src/cache/redis_client.py` |
| 6.1.2 Outbox 轮询派发 | 用 DB 表存储待发消息，Celery 定时任务轮询 HTTP 派发 | `src/core/outbox.py` + `src/tasks/outbox.py` |
| 6.1.3 PostgreSQL → SQLite 开发模式 | 开发/演示时可免安装数据库 | `src/core/database.py` + `docker-compose.dev.yml` |

### 6.2 统一错误处理

```ts
// 目标：消灭所有 catch { /* ignore */ }
catch (e: any) {
  console.error('[Component] action failed:', e?.response?.data ?? e)
  ElMessage.error(e?.response?.data?.detail ?? '操作失败')
}
```

| 任务 | 文件（搜索关键词 `catch {` 或 `catch:`） |
|------|------|
| 6.2.1 前端全覆盖 | `InventoryList.vue`, `AddressList.vue`, `ImportCsv.vue` 等 |
| 6.2.2 后端全覆盖 | `routes/stock.py`, `wms/service.py` 等 |

### 6.3 库存 UI 补全

| 任务 | 内容 | 文件 |
|------|------|------|
| 6.3.1 入库页面 | 采购入库 + 手动入库表单 | `frontend/src/views/stock/StockIn.vue`（新建） |
| 6.3.2 出库页面 | 销售出库 + 调拨出库表单 | `frontend/src/views/stock/StockOut.vue`（新建） |
| 6.3.3 库存调整页面 | 盘盈/盘亏操作 | `frontend/src/views/stock/AdjustStock.vue`（新建） |
| 6.3.4 注册路由 | 添加到 `router/index.ts` | `router/index.ts` |

### 6.4 Amazon/Shopify 配置页面

| 任务 | 内容 | 文件 |
|------|------|------|
| 6.4.1 Amazon 配置 | Access Key / Secret / Marketplaces | `frontend/src/views/connectors/AmazonConfig.vue`（新建） |
| 6.4.2 Shopify 配置 | Shop URL / API Token | `frontend/src/views/connectors/ShopifyConfig.vue`（新建） |
| 6.4.3 注册路由 | 添加 `/connectors/amazon-config` 等路由 | `router/index.ts` |

### 6.5 前端 chunk 优化

| 任务 | 内容 | 文件 |
|------|------|------|
| 6.5.1 按需导入 Element Plus | `unplugin-element-plus` 自动按需导入 | `vite.config.ts` |
| 6.5.2 验证打包结果 | 确保 main chunk < 300KB | `npm run build` |

---

## 🟠 Phase 7 — 电子面单（高优，Week 3-4）

### 7.1 快递鸟/快递 100 对接

这是中小物流最核心的缺口——没有电子面单等于无法发货。

| 任务 | 内容 | 文件 |
|------|------|------|
| 7.1.1 快递鸟 SDK | 封装电子面单 API（下单/查单/打印） | `src/logistics/kdniao.py`（新建） |
| 7.1.2 快递 100 SDK | 备选供应商 | `src/logistics/kuaidi100.py`（新建） |
| 7.1.3 电子面单路由 | 下单/获取面单 URL/打印回调 | `src/logistics/router.py`（新建） |
| 7.1.4 前端面单打印 | 打印预览 + 蓝牙打印机对接 | `frontend/src/views/transport/WaybillPrint.vue`（新建） |

**快递鸟费用**: 约 2000 元/年（电子面单 + 物流查询）

### 7.2 面单模板管理

| 任务 | 内容 | 文件 |
|------|------|------|
| 7.2.1 模板 CRUD | 按快递公司管理面单模板 | `frontend/src/views/barcode/LabelTemplates.vue` |
| 7.2.2 批量打印 | 选中订单 → 批量获取面单 → 批量打印 | `frontend/src/views/transport/BatchPrint.vue`（新建） |

---

## 🟡 Phase 8 — 快递接口真实对接（高优，Week 5-6）

### 8.1 快递公司 API 对接

将当前的 mock 替换为真实 API 调用。

| 任务 | 内容 | 优先级 |
|------|------|--------|
| 8.1.1 顺丰丰桥 API | 下单/查单/路由推送 | P0 |
| 8.1.2 中通开放平台 | 下单/查单 | P0 |
| 8.1.3 圆通/申通/韵达 | 下单/查单 | P1 |
| 8.1.4 EMS/京东 | 下单/查单 | P1 |

### 8.2 运费计算引擎

| 任务 | 内容 | 文件 |
|------|------|------|
| 8.2.1 运费模板 | 按重量/件数/距离阶梯计价 | `src/logistics/rating.py`（新建） |
| 8.2.2 偏远地区附加费 | 新疆/西藏/海南等额外费率 | `src/logistics/surcharge.py`（新建） |
| 8.2.3 前端运费试算 | 发货地→目的地→重量 → 实时报价 | `frontend/src/views/transport/FreightQuote.vue`（新建） |

---

## 🟤 Phase 9 — 派件管理（中优，Week 7-8）

### 9.1 网点/站点管理

| 任务 | 内容 | 文件 |
|------|------|------|
| 9.1.1 网点 CRUD | 网点名称/地址/联系人/区域 | `src/logistics/station.py`（新建） |
| 9.1.2 派件员管理 | 派件员分配/签到/工作量统计 | `src/logistics/courier.py`（新建） |
| 9.1.3 区域划分 | 网点→街道映射，支持自动分单 | `src/logistics/zone.py`（新建） |

### 9.2 派件全流程

| 任务 | 内容 | 文件 |
|------|------|------|
| 9.2.1 到件扫描 | 到达网点扫描 → 自动分配派件员 | `frontend/src/views/transport/ArrivalScan.vue` |
| 9.2.2 派件签收 | 拍照 + 签名 + 签收时间 | `frontend/src/views/transport/DeliveryConfirm.vue` |
| 9.2.3 问题件处理 | 拒收/改址/退回/滞留 | `frontend/src/views/transport/ExceptionHandle.vue` |

### 9.3 PDA 派件端

| 任务 | 内容 | 文件 |
|------|------|------|
| 9.3.1 派件列表 | 今日待派 → 按优先级排序 | `mobile/`（React Native） |
| 9.3.2 签收拍照 | 调用相机 + 签名板 | `mobile/` |
| 9.3.3 离线缓存 | 无网络时本地存储 → 有网自动同步 | `mobile/` + `src/pda/` |

---

## 🟢 Phase 10 — 通知系统（中优，Week 7-8）

### 10.1 短信通知

| 任务 | 内容 | 文件 |
|------|------|------|
| 10.1.1 阿里云短信 SDK | 发货/派件/签收短信 | `src/notification/sms.py`（新建） |
| 10.1.2 短信模板管理 | 后台配置模板 | `src/admin/` |

### 10.2 微信通知

| 任务 | 内容 | 文件 |
|------|------|------|
| 10.2.1 微信公众号/小程序接入 | 订单状态变更推送 | `src/notification/wechat.py`（新建） |
| 10.2.2 微信模板消息 | 发货通知/签收通知 | `src/notification/` |

### 10.3 语音通知（可选）

适用于到件提醒、异常件通知。

| 任务 | 内容 | 文件 |
|------|------|------|
| 10.3.1 阿里云语音 | 到件语音提醒 | `src/notification/voice.py`（新建） |

---

## 🔵 Phase 11 — 部署 & 运维（持续，Week 1-8）

### 11.1 一键部署脚本

| 任务 | 内容 |
|------|------|
| 11.1.1 Docker Compose 生产版 | 含 PostgreSQL/Redis/Nginx 的反向代理/SSL |
| 11.1.2 启动脚本 | `./deploy.sh` — 环境检查 → 数据库初始化 → 启动 |
| 11.1.3 备份脚本 | pg_dump 定时备份 + 7 天轮换 |

### 11.2 监控告警

| 任务 | 内容 |
|------|------|
| 11.2.1 Sentry 配置 | 错误捕获 + 性能监控（免费版足够） |
| 11.2.2 接口健康检查 | `/api/v1/health` 包含各依赖状态（DB/Redis/MQ） |
| 11.2.3 日志轮转 | ELK 或简单按天切割 |

### 11.3 安全加固

| 任务 | 内容 |
|------|------|
| 11.3.1 HTTPS | Let's Encrypt + Nginx 自动续签 |
| 11.3.2 JWT 黑名单 | 登出/修改密码时令牌失效 |
| 11.3.3 操作审计日志 | 所有写操作记录操作人/时间/内容 |

---

## ⚪ Phase 12 — 测试覆盖（持续，Week 1-8）

### 12.1 库存模块测试

| 任务 | 内容 | 文件 |
|------|------|------|
| 12.1.1 Stock In 测试 | 创建/查询/状态转换 | `tests/test_stock_in.py`（新建） |
| 12.1.2 Stock Out 测试 | 创建/查询/状态转换 | `tests/test_stock_out.py`（新建） |
| 12.1.3 Adjust Stock 测试 | 正负调整/记录审计 | `tests/test_stock_adjust.py`（新建） |

### 12.2 E2E 测试（Playwright）

| 任务 | 内容 | 文件 |
|------|------|------|
| 12.2.1 核心流程 E2E | 登录 → 创建订单 → 库存操作 → 运输 → 通知 | `tests/test_e2e/` |
| 12.2.2 PDA 扫码流程 | PDA 登录 → 扫码 → 入库 → 确认 | `tests/test_e2e/` |

### 12.3 前端组件测试

| 任务 | 内容 | 文件 |
|------|------|------|
| 12.3.1 核心组件 | DataTable/SearchBar/StatusTag 等单元测试 | `frontend/src/components/__tests__/` |

---

## 开发排期总表

| Phase | 内容 | 预估工作量 | 依赖 | 优先级 |
|-------|------|-----------|------|--------|
| **6** | 生产加固（降级/错误处理/UI 补全/chunk） | ~10 天 | 无 | 🔴 P0 |
| **7** | 电子面单（快递鸟/打印） | ~8 天 | Phase 6 | 🔴 P0 |
| **8** | 快递接口真实对接 | ~10 天 | Phase 7 | 🟡 P1 |
| **9** | 派件管理（网点/派件员/PDA） | ~10 天 | Phase 6 | 🟤 P2 |
| **10** | 通知系统（短信/微信） | ~5 天 | 无 | 🟢 P2 |
| **11** | 部署/运维 | ~5 天（持续） | 无 | 🔵 P1 |
| **12** | 测试覆盖 | ~5 天（持续） | 无 | ⚪ P1 |

**总计**: ~45-53 天（约 2.5-3 人月）

---

## 硬件 & 服务成本估算

| 项目 | 方案 | 月费/年费 |
|------|------|----------|
| 服务器 | 4C8G 云服务器（阿里云/腾讯云） | ~300 元/月 |
| 数据库 | 使用 PostgreSQL 内嵌，无需额外 RDS | 0 元 |
| 快递鸟 | 电子面单 + 物流查询 | ~2000 元/年 |
| 阿里云短信 | 发货/签收通知（日均 500 单） | ~600 元/年 |
| SSL 证书 | Let's Encrypt 免费 | 0 元 |
| **合计** | | **~500 元/月** |

适合日均 200-2000 单的中小型物流/电商企业。

---

## 建议启动顺序

```
Week 1-2:  Phase 6（加固）+ Phase 11（部署）
               ↓
Week 3-4:  Phase 7（电子面单）← 这是硬性门槛
               ↓
Week 5-6:  Phase 8（快递接口）← 这是价值核心
               ↓
Week 7-8:  Phase 9（派件端）+ Phase 10（通知）
               ↓
持续:      Phase 12（测试覆盖）
```

做完 Phase 7 即可达到 **最小可用产品（MVP）** 状态，可以在一家真实客户试用。
