apiVersion: v1
kind: Service
metadata:
  name: {{ include "tms.fullname" . }}
  labels:
    {{- include "tms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tms.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
