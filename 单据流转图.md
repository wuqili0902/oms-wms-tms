# 全系统单据流转图

## OMS → WMS → TMS 正向流程

```
                        ┌─────────────┐
                        │  OMS 订单   │
                        │             │
                        │ 创建采购单/  │
                        │ 销售订单    │
                        │ (CONFIRMED) │
                        └──┬──┬──┬───┘
                           │  │  │
                    allocate_order         dispatch_transport
                 ┌─────┴──┐        ┌────────┴────────┐
              ▼ WMS · 出库单      ▼ TMS · 运单
              (ALLOCATED)           (CREATED)
              └──┬───────────────┘
                 │ create_picking_wave
           ┌─────┴────┐
           ▼          ▼
     ┌──────────┐  ┌─────────┐
     │ PickingWave│ │ Packing  │
     │ (IN_PROGRESS) │ │(PACKED)  │
     └────┬─────┘  └────┬────┘
           │              │ create_shipment + mark_shipped
           ▼              ▼
     ┌───────────────────────────┐
     │ Shipment (IN_TRANSIT → DELIVERED)│
     └─────────────┬─────────────┘
                   │ POD 签收确认
                   ▼
              ┌────────────┐
              │ ProofOfDelivery│
              │   (SIGNED)  │
              └────────────┘
```

## 退货流程（逆向）

```
  ┌──────────┐     ┌─────────────┐      ┌───────────┐
  │ ReturnOrder│ →  │ ReturnLog  │ →    │ Inventory  │
  │(PENDING)   │     │ (SHIPPED)  │      │ ADJUSTMENT  │
  └──────────┘     └─────────────┘      └───────────┘
```

## TMS 追踪节点（TrackingEvent）

```
PENDING → IN_TRANSIT → TRANSIT_HUB → OUT_FOR_DELIVERY → SIGNED
```

## WMS 设备联动

```
  ┌─────────────┐    ┌────────────┐     ┌──────────────┐
  │TerminalDevice│ →  │SyncLog     │ →   │Mobile Offline │
  │(PDA/手持终端)│     │(心跳同步)   │     │缓存/离线数据   │
  └─────────────┘    └────────────┘     └──────────────┘
```

## RBAC 权限控制（全系统）

```
User → Role → Permission(API/Menu) → Audit Log
```

## WMS StockMovement ↔ InventoryLog

```
  ┌──────────────┐    ┌──────────────┐
  │ StockMovement│ →  │ InventoryLog │
  │ TRANSFER/     │    │ AUDIT LOG   │
  │ REPLENISHMENT │    └──────────────┘
  └──────────────┘
```

## 关键单据状态机

### OMS Order（订单）

| 状态 | 含义 | 触发动作 |
|------|------|----------|
| order_confirmed | 已确认，待分配 | `update_order_status()` |
| allocated | 已分配仓库，WMS 开始拣货 | WMS allocate_order |
| shipped | TMS 承运商取件 | TMS dispatch |
| delivered | POD 签收完成 | TMS mark_signed() |
| returned | 退货完成 | ReturnOrder → Inventory adjustment |

### WMS PickingWave（波次拣货）

| 状态 | 含义 |
|------|------|
| pending | 待创建 |
| in_progress | 拣货进行中（PDA 扫码） |
| packing | 已打包，等待发货 |
| shipped | 出库完成 |

### WMS Shipment（出库单）

| 状态 | 含义 |
|------|------|
| pending | 待生成运单号 |
| in_transit | 承运商取件中 |
| delivered | 签收确认 |

### TMS TransportOrder（运输订单）

| 状态 | 含义 |
|------|------|
| created | 已创建 |
| dispatched | 承运商已接单 |
| in_transit | 运输中 |
| signed | POD 签收完成 |

## 跨系统联动规则

1. **WMS SKU ↔ OMS OrderItem** — WMS 维护商品主数据，OMS 引用 `sku_id`
2. **TMS Tracking → WMS Inventory** — 签收后自动扣减库存（InventoryLog ADJUSTMENT）
3. **ReferenceEntity 泛化关联** — inventory_log 通过 `(type, id)` 关联任何业务记录（customer/order/supplier/picking_wave）
4. **Mobile Sync** — PDA 离线操作 → SyncLog → 主库异步同步，保证数据不丢失
