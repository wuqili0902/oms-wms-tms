# 最新开发计划 — oms-wms-tms

**生成时间**: 2026-07-29  
**项目现状**: ~75% 完成 | **技术栈**: Vue 3 + FastAPI + SQLAlchemy async + pytest

---

## 一、Phase 1（高优）— WMS 库存管理 CRUD

### 任务 1.1: 后端 — 库存增补 (Stock In)
- **文件**: `src/wms/service.py` + `src/wms/router.py`
- **新增端点**: `POST /api/v1/warehouses/{wh_id}/inventory/items`
- **功能**: 
  - 录入商品到仓库（SKU、数量、批次号）
  - 自动创建/更新库存记录
  - 返回操作后的实时库存快照

### 任务 1.2: 后端 — 库存调整 (Stock Adjust)
- **文件**: `src/wms/service.py` + `src/wms/router.py`
- **新增端点**: `PATCH /api/v1/inventory/{item_id}/adjust`
- **功能**: 
  - 盘盈/盘亏调整（正数加、负数减）
  - 记录操作日志

### 任务 1.3: 后端 — 库存调拨 (Transfer)
- **文件**: `src/wms/service.py` + `src/wms/router.py`
- **新增端点**: 
  - `POST /api/v1/inventory/transfers` （创建调拨单）
  - `PATCH /api/v1/inventory/transfers/{id}/status` （确认调入）
- **功能**:
  - 库间调拨（源库位 → 目标库位）
  - 状态机：pending → in_transit → received

### 任务 1.4: 前端 — Inventory Management 页面
- **文件**: `frontend/src/views/warehouses/InventoryList.vue` + `InventoryForm.vue`
- **功能**:
  - 库存列表（分页、筛选仓库/库位/SKU）
  - Stock In / Stock Adjust 表单弹窗
  - Transfer 调拨流程向导

### 任务 1.5: 测试
- `tests/test_wms_service.py` — 补充库存 CRUD 测试用例
- 覆盖：stock-in、adjust（正负）、transfer 状态机

---

## 二、Phase 2（高优）— TMS Route Planning 算法实现

### 任务 2.1: 后端 — 图搜索路径规划
- **文件**: `src/tms/service.py` (route_planning)
- **替换内容**: stub → A* 或 Dijkstra 图搜索
- **输入**: 起终点坐标、途经点、车辆载重/容量约束
- **输出**: RoutePlan.segments（带经纬度序列的路段列表）

### 任务 2.2: 前端 — Route Planning UI
- **文件**: `frontend/src/views/tms/route-plan/RoutePlanning.vue` (新建)
- **功能**: 
  - 地图可视化路径规划（Leaflet / Mapbox GL）
  - 拖拽途经点、自动计算最优路径
  - 查看已有 RoutePlan 详情

### 任务 2.3: 测试
- `tests/test_tms_route_planning.py` — 图搜索算法正确性测试
- 覆盖：单起点终点、多途经点、避障场景

---

## 三、Phase 3（中优）— ML Forecast 真实实现

### 任务 3.1: 后端 — 时间序列预测
- **文件**: `src/tms/ml/forecast.py` (重写) + `src/tml/ml/models.py` (新建)
- **功能**:
  - 使用 Prophet / Statsmodels ARIMA 进行运输量趋势预测
  - 支持按月份/季度训练模型
  - API: `POST /api/v1/ml/forecast/train`, `GET /api/v1/ml/forecast/predict?period=monthly`

### 任务 3.2: 前端 — Forecast 可视化
- **文件**: `frontend/src/views/tms/MlForecast.vue` (新建) + `MlTraining.vue` (新建)
- **功能**: 
  - ECharts 展示预测曲线 vs 历史数据对比
  - Training UI：选择训练区间、查看模型指标

### 任务 3.3: 测试
- `tests/test_ml_forecast.py` — 时间序列算法验证 + API

---

## 四、Phase 4（中优）— Amazon SP-API 完整集成

### 任务 4.1: 后端 — Amazon Connector 扩展
- **文件**: `src/connectors/amazon_mws.py` (重写) → `src/connectors/amazon_sp_api.py`
- **新增功能**:
  - 订单状态同步（order status polling）
  - Tracking 自动回传 + FBA 发货管理
  - Inventory Sync（库存同步到 Amazon）

### 任务 4.2: 前端 — Amazon Config 页面
- **文件**: `frontend/src/views/connectors/amazon-config.vue` (完善)
- **功能**:
  - AWS Access Key / Secret Key 配置
  - MWS Auth Token 绑定
  - Connector 状态查看 + 错误日志

### 任务 4.3: 测试
- `tests/test_amazon_connector.py` — 模拟 SP-API 请求/响应

---

## 五、Phase 5（低优）— 前端 Stub 补全 & 代码质量

### 任务 5.1: Barcode Templates 页面补全
- **文件**: `frontend/src/views/barcode/templates.vue` (完善)
- **功能**: CRUD + Excel Upload 入口 + Template Preview

### 任务 5.2: Connectors Index 页面补全
- **文件**: `frontend/src/views/connectors/index.vue` (完善)
- **功能**: Shopify/Amazon Connector 状态总览 + 配置入口

### 任务 5.3: DeviceDetail.vue 修复
- **文件**: `frontend/src/views/devices/DeviceDetail.vue`
- **功能**: 
  - Session Management（创建、查看、关闭会话）
  - Sync Logs 真实接口调用
  - Error handling 补全

### 任务 5.4: 统一错误处理 (Error Handling)
- **范围**: 全局搜索 `catch { /* ignore */ }` 并替换为 proper error handling
- **标准**: 
  ```ts
  catch (e: any) {
    console.error('[Component] action failed:', e?.response?.data ?? e);
    ElMessage.error(e?.response?.data?.detail ?? '操作失败');
  }
  ```

---

## 六、开发排期建议

| Phase | 预估工作量 | 依赖关系 |
|-------|-----------|----------|
| **Phase 1** — WMS 库存 CRUD | ~3-4天 | 无依赖，可独立开发 |
| **Phase 2** — Route Planning 算法 | ~5-7天 | 需要图数据结构知识 |
| **Phase 3** — ML Forecast | ~4-5天 | 需时间序列基础 |
| **Phase 4** — Amazon SP-API | ~3-4天 | 需 AWS IAM 配置 |
| **Phase 5** — Stub 补全 + QA | ~2-3天 | 可在 Phase 1-4 完成后并行 |

---

## 七、验收标准

每个任务完成后需要满足：
1. ✅ `pytest tests/` 新增用例全部通过（0 失败, 0 skip）
2. ✅ Vue component linting (`vue-tsc --noEmit`) 无 error
3. ✅ API 接口文档同步更新 (FastAPI Swagger UI)
4. ✅ Git commit message 遵循 Conventional Commits 格式

---

*计划生成时间: 2026-07-29 | 基于 PROJECT_COMPLETION_REPORT.md 评估结果*
